/** Cookie key for the JWT access token */
export const ACCESS_TOKEN_KEY = "access_token";

/** Cookie key for the user's role name — plain text, readable by middleware */
export const USER_ROLE_KEY = "user_role";
